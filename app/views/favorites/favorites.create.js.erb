 $(".favorite-btn").html("<%= escape_javascript(render partial: 'favorites/favorite-btn', locals: {user: @user}) %>");
 $(".favorite-btn").html("<%= j(render 'favorites/favorite-btn', user: @user) %>");
